import React from 'react';

const Calculat = () => {
    const add = (f, s) => {
        const t = f + s
        return t
    }

    return (
        <div>

        </div>
    );
};

export default Calculat;